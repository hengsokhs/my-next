import { Alert, AlertTitle } from "@/components/ui/alert";

export default function DashboardPage() {
    return(
        <Alert>
            <AlertTitle className={"text-center text-xl"}>Dashboard</AlertTitle>
        </Alert>
    );
}
import { Alert, AlertTitle } from "@/components/ui/alert";

export default function AboutPage() {
    return(
        <div >
            <Alert>
                <AlertTitle className="text-center text-xl">About</AlertTitle>
            </Alert>
        </div>
    );
}
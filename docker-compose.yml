version: '3.8'

services:
  db:
    image: postgres:16
    container_name: postgres_db
    environment:
      POSTGRES_PASSWORD: '123'
    ports:
      - "5433:5432"
    volumes:
      - "./my_local_data:/var/lib/postgresql/data"